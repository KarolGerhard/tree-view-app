name: tree_view_desafio
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'


dependencies:
  get:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.3.0
  path: ^1.8.3
  animated_tree_view: ^2.2.0
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  sqflite_common_ffi:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/jaguar_unit_json/
    - assets/tobias_unit_json/
    - assets/apex_unit_json/
    - assets/icons/
    - assets/images/
